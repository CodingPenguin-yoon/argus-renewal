{
  "name": "argus-renewal-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "cd backend && python3 -m uvicorn src.main:app --reload --host 0.0.0.0 --port 4000",
    "lint": "pnpm --filter frontend lint && cd backend && python3 -m compileall -q src",
    "test": "pnpm --filter frontend test && cd backend && pytest -q",
    "build": "pnpm --filter frontend build && cd backend && python3 -m compileall -q src"
  }
}
