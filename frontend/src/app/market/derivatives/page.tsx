import styles from "@/market_terminal/shell/market-terminal-shell.module.css";

export default function DerivativesPage() {
  return (
    <section className={styles.placeholder}>
      <p className={styles.placeholderEyebrow}>PLANNED CAPABILITY</p>
      <h1>KOSPI200 파생</h1>
      <p>검증된 KIS fixture를 새 계약으로 이관한 뒤 선물·옵션체인과 투자자 수급을 제공합니다.</p>
    </section>
  );
}

